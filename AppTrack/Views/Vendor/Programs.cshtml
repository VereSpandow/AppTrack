<h2>Vendor Program Update</h2>
<div class="col-md-12">
    @{
        Html.RenderAction("ProgramList", "Vendor", new { VendorID = ViewBag.VendorID });
    }
</div>
<div class="pull-right"><a class="btn btn-info" href="@Url.Action(ViewBag.NextAction, "Vendor", new { VendorID = ViewBag.VendorID })">Continue</a></div>
